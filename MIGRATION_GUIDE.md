# Руководство по миграции на Supabase + Railway

## ✅ Идея отличная!

**Railway + Supabase + Telegram Bot** - это идеальная схема для вашего бизнеса:

### Преимущества:
1. ✅ **Быстрый запуск** - все готово за час
2. ✅ **Низкая стоимость** - можно начать с бесплатных планов
3. ✅ **Масштабируемость** - легко увеличить ресурсы
4. ✅ **Telegram интеграция** - нативная поддержка Web Apps
5. ✅ **Автоматический HTTPS** - Railway предоставляет SSL
6. ✅ **Автоматические бэкапы** - Supabase делает это автоматически

## 📋 Шаги миграции

### Шаг 1: Установка зависимостей

```bash
npm install @supabase/supabase-js
```

### Шаг 2: Настройка Supabase

1. Перейдите в [Supabase Dashboard](https://app.supabase.com)
2. Создайте новый проект (или используйте существующий)
3. Получите ключи:
   - Settings > API > Project URL
   - Settings > API > anon/public key
   - Settings > API > service_role key (секретный!)

### Шаг 3: Создание таблиц

Таблицы уже созданы через миграцию:
- `b2b_products` - товары
- `b2b_orders` - заказы
- `b2b_users` - магазины и супер-админы
- `b2b_customers` - покупатели

### Шаг 4: Настройка переменных окружения

Создайте `.env.local`:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# JWT
JWT_SECRET=your-jwt-secret

# Environment
NODE_ENV=production
```

### Шаг 5: Переключение на Supabase

В `lib/db.ts` замените:

```typescript
// Старый код (JSON файлы)
import { db } from './db';

// Новый код (Supabase)
import { supabaseDb as db } from './supabase';
```

Или создайте файл `lib/db-wrapper.ts`:

```typescript
// Использовать Supabase в production, JSON в development
const useSupabase = process.env.NEXT_PUBLIC_SUPABASE_URL !== undefined;

export const db = useSupabase 
  ? await import('./supabase').then(m => m.supabaseDb)
  : await import('./db').then(m => m.db);
```

### Шаг 6: Деплой на Railway

1. **Подключите GitHub репозиторий:**
   - Зайдите на [railway.app](https://railway.app)
   - New Project > Deploy from GitHub repo
   - Выберите ваш репозиторий

2. **Railway автоматически определит Next.js:**
   - Автоматически установит зависимости
   - Запустит `npm run build`
   - Запустит `npm start`

3. **Настройте переменные окружения:**
   - Variables > Add Variable
   - Добавьте все переменные из `.env.local`

4. **Получите домен:**
   - Railway создаст домен: `your-app.railway.app`
   - Можно настроить кастомный домен

### Шаг 7: Интеграция с Telegram Bot

В вашем Telegram боте:

```javascript
const webAppUrl = 'https://your-app.railway.app';

// Отправка кнопки с Web App
bot.api.sendMessage(chatId, '🛒 Открыть магазин', {
  reply_markup: {
    inline_keyboard: [[
      {
        text: '🛒 Открыть магазин',
        web_app: { url: webAppUrl }
      }
    ]]
  }
});
```

### Шаг 8: Использование Telegram Web App API

В вашем Next.js приложении можно использовать Telegram API:

```typescript
// В компоненте
'use client';

useEffect(() => {
  if (window.Telegram?.WebApp) {
    const tg = window.Telegram.WebApp;
    tg.ready();
    tg.expand();
    
    // Получить данные пользователя
    const user = tg.initDataUnsafe?.user;
    console.log('Telegram user:', user);
  }
}, []);
```

## 💰 Стоимость

### Бесплатные планы (для старта):
- **Railway**: $5 кредитов/месяц (достаточно для MVP)
- **Supabase**: 500MB БД, 2GB bandwidth

### Платные планы (при росте):
- **Railway Pro**: $20/месяц (2GB RAM, 10GB storage)
- **Supabase Pro**: $25/месяц (8GB БД, 50GB bandwidth)

**Итого для старта: ~$0-5/месяц**

## 🔒 Безопасность

1. **Row Level Security (RLS)** в Supabase:
   - Настройте политики для защиты данных
   - Магазины видят только свои товары

2. **Service Role Key**:
   - Храните только на сервере (Railway variables)
   - Никогда не используйте в клиентском коде

3. **HTTPS**:
   - Railway предоставляет автоматически
   - Обязателен для Telegram Web Apps

## 📊 Мониторинг

- **Railway**: Метрики CPU, RAM, Network
- **Supabase**: Dashboard с метриками БД, запросов
- **Telegram**: Статистика бота в BotFather

## ✅ Чеклист перед запуском

- [ ] Установлен `@supabase/supabase-js`
- [ ] Созданы таблицы в Supabase
- [ ] Настроены переменные окружения
- [ ] Код переключен на Supabase
- [ ] Проект задеплоен на Railway
- [ ] Получен Railway домен
- [ ] Домен добавлен в Telegram Bot
- [ ] Протестирована работа Web App

## 🚀 Готово!

После выполнения всех шагов ваш сайт будет:
- ✅ Работать на Railway с автоматическим HTTPS
- ✅ Использовать Supabase как БД
- ✅ Открываться внутри Telegram бота
- ✅ Масштабироваться при росте бизнеса

Удачи с запуском! 🎉

