# 🚀 Быстрый старт Supabase

## ✅ Достаточно ли одного файла?

**ДА!** Достаточно запустить только `001_initial_schema.sql` в SQL Editor Supabase.

## 📝 Пошаговая инструкция

### 1. Откройте Supabase Dashboard
- Зайдите на [supabase.com](https://supabase.com)
- Откройте ваш проект

### 2. Откройте SQL Editor
- В левом меню выберите **SQL Editor**
- Нажмите **New Query**

### 3. Скопируйте и выполните миграцию
- Откройте файл `supabase/migrations/001_initial_schema.sql`
- Скопируйте **весь** содержимое файла
- Вставьте в SQL Editor
- Нажмите **Run** (или `Ctrl+Enter` / `Cmd+Enter`)

### 4. Проверьте результат
Должно появиться сообщение: **Success. No rows returned**

### 5. Проверьте таблицы
- Перейдите в **Table Editor**
- Должны появиться 4 таблицы:
  - ✅ `b2b_users`
  - ✅ `b2b_customers`
  - ✅ `b2b_products`
  - ✅ `b2b_orders`

## ⚙️ Настройка переменных окружения

После создания таблиц, получите ключи:

1. Перейдите в **Settings > API**
2. Скопируйте:
   - **Project URL**: `https://your-project.supabase.co`
   - **anon/public key**: для клиентских запросов
   - **service_role key**: для backend (⚠️ НЕ ПУБЛИКУЙТЕ!)

3. Добавьте в Railway (или `.env`):
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

## 🎯 Что делает миграция?

✅ Создает 4 таблицы с правильной структурой  
✅ Добавляет индексы для производительности  
✅ Настраивает автоматическое обновление `updated_at`  
✅ Включает Row Level Security (RLS)  
✅ Создает публичные политики для чтения товаров и магазинов  
✅ Настраивает политики для работы с Service Role Key  

## ⚠️ Важно

- **Service Role Key** обходит все RLS политики
- Backend использует Service Role Key, поэтому все операции будут работать
- RLS политики здесь для дополнительной защиты при прямых запросах к БД
- Публичный доступ настроен для чтения товаров и магазинов (каталог)

## 🧪 Тестирование

После применения миграции, создайте первого супер-админа:

```sql
-- Создайте хеш пароля через bcrypt (или используйте скрипт init-admin)
INSERT INTO b2b_users (username, password_hash, role)
VALUES (
  'admin',
  '$2a$10$...', -- замените на реальный хеш
  'super-admin'
);
```

Или используйте скрипт:
```bash
cd backend
npm run init-admin
```

## ✅ Готово!

После выполнения этих шагов, ваш проект готов к работе! 🎉





