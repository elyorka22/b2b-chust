# Supabase Database Setup

## 📋 Быстрая настройка

### 1. Создайте проект на Supabase

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь завершения инициализации

### 2. Выполните миграции

#### Вариант A: Через Supabase Dashboard (SQL Editor)

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `migrations/001_initial_schema.sql`
4. Вставьте в SQL Editor и выполните (Run)

#### Вариант B: Через Supabase CLI

```bash
# Установите Supabase CLI (если еще не установлен)
npm install -g supabase

# Войдите в Supabase
supabase login

# Инициализируйте проект (если еще не инициализирован)
supabase init

# Свяжите с вашим проектом
supabase link --project-ref your-project-ref

# Примените миграции
supabase db push
```

### 3. Получите ключи доступа

В Supabase Dashboard перейдите в **Settings > API**:

- **Project URL**: `https://your-project.supabase.co`
- **anon/public key**: Используется для клиентских запросов
- **service_role key**: Используется для серверных запросов (⚠️ НЕ ПУБЛИКУЙТЕ!)

### 4. Настройте переменные окружения

Добавьте в Railway (или `.env` для локальной разработки):

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

## 📊 Структура базы данных

### Таблицы

1. **b2b_users** - Пользователи (магазины и супер-админы)
2. **b2b_customers** - Покупатели
3. **b2b_products** - Товары
4. **b2b_orders** - Заказы

### Row Level Security (RLS)

RLS включен для всех таблиц. Политики настроены так:
- **Публичный доступ**: Чтение товаров и магазинов
- **Авторизованный доступ**: Создание/обновление через Service Role Key

## 🔒 Безопасность

### Service Role Key

⚠️ **ВАЖНО**: Service Role Key обходит все RLS политики и имеет полный доступ к БД.

- Используйте только в **backend** сервере
- **НИКОГДА** не публикуйте в клиентском коде
- Храните в переменных окружения

### RLS Политики

Если вы используете Service Role Key в backend (как в нашем случае), RLS политики работают как дополнительный слой защиты для прямых запросов к БД.

## 🧪 Тестирование

### Создание первого супер-админа

После настройки БД, создайте первого супер-админа через скрипт:

```bash
cd backend
npm run init-admin
```

Или через SQL:

```sql
INSERT INTO b2b_users (username, password_hash, role)
VALUES (
  'admin',
  '$2a$10$...', -- хеш пароля (используйте bcrypt)
  'super-admin'
);
```

## 📝 Миграции

- `001_initial_schema.sql` - Основная схема БД с RLS
- `002_simplified_rls.sql` - Упрощенные RLS политики (опционально)

## 🔄 Обновление схемы

При изменении схемы БД:

1. Создайте новый файл миграции: `003_your_changes.sql`
2. Примените через SQL Editor или CLI
3. Обновите код в `backend/server.js` и `frontend/lib/db.ts`

## 📚 Дополнительные ресурсы

- [Supabase Documentation](https://supabase.com/docs)
- [Row Level Security Guide](https://supabase.com/docs/guides/auth/row-level-security)
- [PostgreSQL JSONB](https://www.postgresql.org/docs/current/datatype-json.html)

