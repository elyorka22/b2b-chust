# B2B Chust - Оптовый магазин

B2B веб-сервис для оптовых закупок с системой управления заказами и товарами.

## Возможности

### Для клиентов (без регистрации)
- Просмотр каталога товаров
- Добавление товаров в корзину
- Оформление заказа через форму (номер телефона и адрес)

### Для администраторов
- Просмотр поступающих заказов
- Управление статусами заказов

### Для супер-администраторов
- Управление товарами (создание, редактирование, удаление)
- Просмотр всех заказов
- Создание новых администраторов
- Просмотр статистики (выручка, количество заказов, товары с низким остатком)

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте первого супер-админа:
```bash
npm run init-admin
# или с кастомными данными:
npm run init-admin -- username password
```

3. Запустите сервер разработки:
```bash
npm run dev
```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Структура проекта

```
/app
  /api          # API маршруты
  /admin        # Админ-панель
  /cart         # Страница корзины
  page.tsx      # Главная страница
/components     # React компоненты
/lib            # Утилиты и база данных
/scripts        # Скрипты инициализации
```

## Использование

### Вход в админ-панель
1. Перейдите на `/admin/login`
2. Введите имя пользователя и пароль
3. После входа вы будете перенаправлены в админ-панель

### Создание товара (супер-админ)
1. Войдите как супер-админ
2. Перейдите на вкладку "Товары"
3. Нажмите "Добавить товар"
4. Заполните форму и сохраните

### Создание администратора (супер-админ)
1. Войдите как супер-админ
2. Перейдите на вкладку "Пользователи"
3. Нажмите "Создать пользователя"
4. Заполните форму (имя пользователя, пароль, роль)

## Технологии

- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS
- JWT для аутентификации
- bcrypt для хеширования паролей
- JSON файлы для хранения данных (development)
- Supabase для базы данных (production)
- Telegram Bot API для уведомлений

## Примечания

- **Development**: Данные хранятся в папке `data/` в формате JSON
- **Production**: Используется Supabase (PostgreSQL) - см. `MIGRATION_GUIDE.md`
- JWT секрет по умолчанию - измените его в `.env` для production

## 🚀 Деплой на Railway + Supabase

Для продакшена рекомендуется использовать:
- **Railway** для хостинга Next.js приложения
- **Supabase** для базы данных PostgreSQL

Подробные инструкции в файле `MIGRATION_GUIDE.md`

### Быстрый старт для production:

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Настройте Supabase (см. `MIGRATION_GUIDE.md`)

3. Создайте `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   JWT_SECRET=your-jwt-secret
   ```

4. Деплой на Railway:
   - Подключите GitHub репозиторий
   - Railway автоматически определит Next.js
   - Добавьте переменные окружения
   - Получите домен: `your-app.railway.app`

5. Используйте Railway домен в Telegram Bot как Web App URL

